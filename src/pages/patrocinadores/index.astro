---
import Layout from "@/layouts/Layout.astro";
import SponsorCard from "@/components/SponsorCard.astro";
import BenefitCard from "@/components/BenefitCard.astro";

import featured from "@/data/sponsors/featured.json";
import sponsors from "@/data/sponsors/sponsors.json";
import benefits from "@/data/benefits/why-sponsor.json";

import { Users, Target, Zap, Heart, BriefcaseBusiness } from "@lucide/astro";
import CallToActionSection from "@/components/CallToActionSection.astro";
import Button from "@/components/Button.astro";

const heroStats = [
  { value: "15+", text: "Patrocinadores Activos", color: "text-google-blue" },
  { value: "$50k", text: "Apoyo Anual Total", color: "text-google-red" },
  { value: "100+", text: "Eventos Patrocinados", color: "text-google-yellow" },
  {
    value: "5000+",
    text: "Desarrolladores Alcanzados",
    color: "text-google-green",
  },
];

const benefitIconMap = { Users, Target, Zap, Heart };

const metadata = {
  title: "Patrocinadores - Google Developer Group Ica",
  description:
    "Conoce a nuestros patrocinadores y descubre cómo puedes apoyar al GDG ICA",
  image: "",
  url: "https://gdgica.com/patrocinadores",
};
---

<Layout {...metadata}>
  <section
    class="relative isolate overflow-hidden bg-gradient-to-b from-blue-50 to-green-50 py-20"
  >
    <div class="container text-center">
      <p
        class="mb-6 inline-flex h-24 w-24 items-center justify-center rounded-2xl bg-gradient-to-r from-blue-600 via-red-500 to-green-600"
      >
        <BriefcaseBusiness stroke-width={1.5} size={64} color="#FFFFFF" />
      </p>
      <h1 class="mb-6 text-5xl font-bold">
        <span class="text-google-blue">Nuestros </span>
        <span class="text-google-red">Patrocinadores</span>
      </h1>
      <p class="text-secondary mx-auto mb-12 max-w-3xl text-xl">
        Agradecemos profundamente a las empresas y organizaciones que hacen
        posible nuestra misión de democratizar el acceso al conocimiento
        tecnológico. Su apoyo transforma vidas y construye el futuro digital.
      </p>
      <div class="mx-auto grid max-w-3xl grid-cols-2 gap-6 sm:grid-cols-4">
        {
          heroStats.map((s) => (
            <div class="flex flex-col items-center">
              <p class={"text-3xl font-bold " + s.color}>{s.value}</p>
              <p class="text-secondary text-center text-sm">{s.text}</p>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <section class="container py-24">
    <div class="mx-auto mb-14 max-w-2xl text-center">
      <h2 class="mb-4 text-3xl font-bold">Patrocinadores Principales</h2>
      <p class="text-secondary text-xl">
        Nuestros socios estratégicos que impulsan la innovación y el crecimiento
        de la comunidad
      </p>
    </div>
    <div class="mx-auto grid max-w-4xl grid-cols-1 gap-8 md:grid-cols-2">
      {featured.map((f) => <SponsorCard {...f} />)}
    </div>
  </section>

  <section class="bg-section py-24">
    <div class="container">
      <div class="mx-auto mb-14 max-w-2xl text-center">
        <h2 class="mb-4 text-3xl font-bold">Todos Nuestros Patrocinadores</h2>
        <p class="text-secondary text-xl">
          Cada empresa que nos apoya es fundamental para nuestro éxito
        </p>
      </div>
      <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">
        {sponsors.map((s) => <SponsorCard {...s} />)}
      </div>
    </div>
  </section>

  <section class="container py-24">
    <div class="mx-auto mb-14 max-w-3xl text-center">
      <h2 class="mb-4 text-3xl font-bold">¿Por Qué Patrocinar GDG?</h2>
      <p class="text-secondary text-xl">
        Los beneficios tangibles de asociarse con la comunidad de
        desarrolladores más activa de la región
      </p>
    </div>
    <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-4">
      {
        benefits.map((b) => (
          <BenefitCard
            title={b.title}
            description={b.description}
            icon={benefitIconMap[b.icon]}
            color={b.color}
          />
        ))
      }
    </div>
  </section>
  <CallToActionSection>
    <h2 class="mb-6 text-4xl font-bold text-white">
      ¿Quieres Ser Nuestro Próximo Patrocinador?
    </h2>
    <p class="mb-8 text-xl text-white">
      Únete a las empresas líderes que apoyan el crecimiento del ecosistema
      tecnológico. Juntos podemos formar a los desarrolladores del futuro y
      crear un impacto duradero en la industria.
    </p>
    <Button
      variant="white"
      href="https://forms.gle/tu-formulario"
      target="_blank"
    >
      <BriefcaseBusiness />
      Quiero ser patrocinador
    </Button>
  </CallToActionSection>
</Layout>
