---
const { image } = Astro.props;
---

<figure data-fancybox="gallery" data-src={image.src} data-caption={image.src}>
  <img
    src={image.src}
    class="mb-4 h-auto w-full rounded-lg transition hover:scale-98 hover:cursor-pointer"
    width="200"
    height="150"
    alt={image.alt}
  />
</figure>
