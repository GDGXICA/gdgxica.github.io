---
const { title, description, icon: Icon, color } = Astro.props;
---

<div
  class="flex flex-col gap-3 rounded-2xl border border-gray-200 p-6 text-center shadow-sm"
>
  <div
    class="mx-auto flex h-12 w-12 items-center justify-center rounded-full bg-gray-50"
  >
    <Icon stroke-width={1.5} class="h-6 w-6" color={color} />
  </div>
  <h3 class="text-primary text-base font-semibold">{title}</h3>
  <p class="text-secondary text-sm">{description}</p>
</div>
